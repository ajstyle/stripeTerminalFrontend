// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyDbUDEC4qKRLeYYlNcTCkjvXQT1rPOSmf0',
    authDomain: 'taqueria-chavez.firebaseapp.com',
    databaseURL: 'https://taqueria-chavez.firebaseio.com',
    projectId: 'taqueria-chavez',
    storageBucket: 'taqueria-chavez.appspot.com',
    messagingSenderId: '290513982025',
    appId: '1:290513982025:web:8725e0d7a830a5b2'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
